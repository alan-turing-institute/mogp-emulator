language: python
python:
  - "3.7"
install:
  - pip install -r requirements.txt
  - pip install pytest-cov codecov patsy
  - python setup.py install
script:
  - pytest --cov=./
after_success:
  - codecov
